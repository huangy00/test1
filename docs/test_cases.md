# 测试用例清单

## 一、用户模块

### 1.1 注册接口测试

| 用例ID | 用例名称 | 前置条件 | 测试步骤 | 预期结果 | 优先级 |
|--------|---------|---------|---------|---------|--------|
| TC-REG-001 | 正常注册 | 无 | POST /api/register，传入合法参数 | 注册成功，返回200 | P0 |
| TC-REG-002 | 邮箱已存在 | 邮箱已注册 | POST /api/register，使用已存在邮箱 | 返回错误提示 | P0 |
| TC-REG-003 | 邮箱为空 | 无 | POST /api/register，email字段为空 | 返回参数错误 | P0 |
| TC-REG-004 | 密码为空 | 无 | POST /api/register，password字段为空 | 返回参数错误 | P0 |
| TC-REG-005 | 邮箱格式错误 | 无 | POST /api/register，email不包含@ | 返回参数错误 | P1 |
| TC-REG-006 | 密码过短 | 无 | POST /api/register，password少于4位 | 返回参数错误 | P1 |
| TC-REG-007 | SQL注入 | 无 | POST /api/register，email字段包含SQL语句 | 返回参数错误，无SQL异常 | P0 |

### 1.2 登录接口测试

| 用例ID | 用例名称 | 前置条件 | 测试步骤 | 预期结果 | 优先级 |
|--------|---------|---------|---------|---------|--------|
| TC-LOGIN-001 | 正常登录 | 用户已注册 | POST /api/login，正确邮箱密码 | 登录成功，返回token | P0 |
| TC-LOGIN-002 | 密码错误 | 用户已注册 | POST /api/login，错误密码 | 返回认证失败 | P0 |
| TC-LOGIN-003 | 账号不存在 | 无 | POST /api/login，未注册的邮箱 | 返回认证失败 | P0 |
| TC-LOGIN-004 | 邮箱为空 | 无 | POST /api/login，email为空 | 返回参数错误 | P0 |
| TC-LOGIN-005 | 密码为空 | 无 | POST /api/login，password为空 | 返回参数错误 | P0 |
| TC-LOGIN-006 | SQL注入 | 无 | POST /api/login，email包含SQL语句 | 返回参数错误 | P0 |

### 1.3 注册UI测试

| 用例ID | 用例名称 | 测试步骤 | 预期结果 | 优先级 |
|--------|---------|---------|---------|--------|
| TC-UI-REG-001 | 正常注册 | 填写表单并提交 | 跳转到成功页面 | P0 |
| TC-UI-REG-002 | 必填项为空 | 不填写直接提交 | 显示错误提示 | P0 |
| TC-UI-REG-003 | 密码不一致 | 两次密码不同 | 显示错误提示 | P1 |
| TC-UI-REG-004 | 勾选隐私政策 | 不勾选直接提交 | 提交失败 | P1 |

### 1.4 登录UI测试

| 用例ID | 用例名称 | 测试步骤 | 预期结果 | 优先级 |
|--------|---------|---------|---------|--------|
| TC-UI-LOGIN-001 | 正常登录 | 输入正确邮箱密码 | 跳转到账户页面 | P0 |
| TC-UI-LOGIN-002 | 密码错误 | 输入错误密码 | 显示错误提示 | P0 |
| TC-UI-LOGIN-003 | 账号不存在 | 输入未注册邮箱 | 显示错误提示 | P0 |
| TC-UI-LOGIN-004 | 空表单提交 | 不填写直接提交 | 显示错误提示 | P1 |

## 二、商品模块

### 2.1 商品搜索接口测试

| 用例ID | 用例名称 | 测试步骤 | 预期结果 | 优先级 |
|--------|---------|---------|---------|--------|
| TC-SRCH-001 | 正常搜索 | GET /api/product/search?search=MacBook | 返回包含MacBook的商品列表 | P0 |
| TC-SRCH-002 | 空结果搜索 | GET /api/product/search?search=不存在的商品xyz | 返回空列表 | P0 |
| TC-SRCH-003 | 空关键词搜索 | GET /api/product/search?search= | 返回全部商品或提示错误 | P1 |
| TC-SRCH-004 | SQL注入搜索 | GET /api/product/search?search=' OR 1=1 -- | 返回空列表或错误，无SQL异常 | P0 |
| TC-SRCH-005 | 特殊字符搜索 | GET /api/product/search?search=<script>alert(1)</script> | 返回空列表，无XSS执行 | P0 |

### 2.2 商品搜索UI测试

| 用例ID | 用例名称 | 测试步骤 | 预期结果 | 优先级 |
|--------|---------|---------|---------|--------|
| TC-UI-SRCH-001 | 正常搜索 | 在搜索框输入关键词并回车 | 显示搜索结果 | P0 |
| TC-UI-SRCH-002 | 空结果搜索 | 搜索不存在的商品 | 显示"没有商品"提示 | P0 |
| TC-UI-SRCH-003 | 空关键词搜索 | 搜索框为空直接搜索 | 显示全部商品或提示 | P1 |

## 三、购物车模块

### 3.1 加购接口测试

| 用例ID | 用例名称 | 测试步骤 | 预期结果 | 优先级 |
|--------|---------|---------|---------|--------|
| TC-CART-001 | 正常加购 | POST /api/cart/add，传入product_id和quantity | 加购成功 | P0 |
| TC-CART-002 | 数量为0 | POST /api/cart/add，quantity=0 | 返回参数错误 | P0 |
| TC-CART-003 | 数量为负数 | POST /api/cart/add，quantity=-1 | 返回参数错误 | P0 |
| TC-CART-004 | 超出库存 | POST /api/cart/add，quantity大于库存 | 返回库存不足 | P0 |
| TC-CART-005 | 商品不存在 | POST /api/cart/add，product_id不存在 | 返回商品不存在 | P1 |

### 3.2 购物车UI测试

| 用例ID | 用例名称 | 测试步骤 | 预期结果 | 优先级 |
|--------|---------|---------|---------|--------|
| TC-UI-CART-001 | 加购商品 | 点击"Add to Cart" | 显示成功提示，购物车数量+1 | P0 |
| TC-UI-CART-002 | 查看购物车 | 点击购物车图标 | 显示购物车商品列表 | P0 |
| TC-UI-CART-003 | 修改数量 | 在购物车中修改商品数量 | 数量更新，总价重新计算 | P1 |
| TC-UI-CART-004 | 删除商品 | 点击删除按钮 | 商品移除，总价更新 | P1 |

## 四、订单模块

### 4.1 下单接口测试

| 用例ID | 用例名称 | 测试步骤 | 预期结果 | 优先级 |
|--------|---------|---------|---------|--------|
| TC-ORD-001 | 正常下单 | POST /api/checkout，传入合法参数 | 下单成功，返回订单号 | P0 |
| TC-ORD-002 | 购物车为空 | POST /api/checkout，购物车为空 | 返回错误提示 | P0 |
| TC-ORD-003 | 地址为空 | POST /api/checkout，address为空 | 返回参数错误 | P0 |
| TC-ORD-004 | 库存不足 | POST /api/checkout，商品库存不足 | 返回库存不足错误 | P0 |
| TC-ORD-005 | 未登录下单 | 不传token，POST /api/checkout | 返回未授权 | P0 |

### 4.2 订单UI测试

| 用例ID | 用例名称 | 测试步骤 | 预期结果 | 优先级 |
|--------|---------|---------|---------|--------|
| TC-UI-ORD-001 | 完整下单流程 | 搜索→加购→结算→确认 | 订单创建成功 | P0 |
| TC-UI-ORD-002 | 选择支付方式 | 点击"Choose"选择支付方式 | 显示可用支付方式 | P0 |
| TC-UI-ORD-003 | 确认订单 | 选择支付方式后点击确认 | 订单提交成功 | P0 |
| TC-UI-ORD-004 | 未选支付方式 | 不选支付方式直接确认 | 按钮禁用或提示错误 | P1 |

## 五、数据库一致性测试

| 用例ID | 用例名称 | 测试步骤 | 预期结果 | 优先级 |
|--------|---------|---------|---------|--------|
| TC-DB-001 | 用户注册后数据落库 | 注册后查oc_customer表 | 用户记录存在且字段正确 | P0 |
| TC-DB-002 | 订单创建后数据落库 | 下单后查oc_order表 | 订单记录存在且金额正确 | P0 |
| TC-DB-003 | 订单商品关联 | 下单后查oc_order_product表 | 关联记录存在 | P0 |
| TC-DB-004 | 库存扣减 | 下单后查oc_product表quantity | 库存已扣减 | P0 |
| TC-DB-005 | 支付状态一致 | 支付后查oc_order.status | 订单状态变为已支付 | P1 |

## 六、用例统计

| 模块 | 接口测试 | UI测试 | 数据库测试 | 合计 |
|------|---------|--------|-----------|------|
| 用户模块 | 13 | 8 | 1 | 22 |
| 商品模块 | 5 | 3 | 0 | 8 |
| 购物车模块 | 5 | 4 | 0 | 9 |
| 订单模块 | 5 | 4 | 0 | 9 |
| 数据库测试 | 0 | 0 | 5 | 5 |
| **合计** | **28** | **19** | **5** | **53** |
